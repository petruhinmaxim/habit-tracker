# Быстрый старт

## 1. Установка зависимостей

```bash
npm install
```

## 2. Настройка переменных окружения

Скопируйте `env.template` в `.env` и заполните:

```bash
# Windows
copy env.template .env

# Linux/Mac
cp env.template .env
```

**Обязательно укажите:**
- `BOT_TOKEN` - получите у [@BotFather](https://t.me/BotFather) в Telegram

## 3. Запуск Docker контейнеров

```bash
npm run docker:up
```

Подождите 10-15 секунд, пока PostgreSQL и Redis запустятся.

## 4. Настройка базы данных

```bash
npm run db:generate
npm run db:migrate
```

## 5. Запуск проекта

```bash
npm run dev
```

Теперь:
- Бот работает в Telegram
- WebApp доступен по адресу http://localhost:3000

## Проверка работы

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start`
3. Вы увидите приветственное сообщение с кнопками
4. Нажмите "📱 Открыть WebApp"
5. Введите текст в поле и отправьте
6. Текст появится в боте

## Остановка

```bash
# Остановить бота и веб-приложение: Ctrl+C
# Остановить Docker контейнеры:
npm run docker:down
```

