{
  "name": "habit-tracker-bot",
  "version": "1.0.0",
  "description": "Telegram bot for motivation and workout tracking",
  "private": true,
  "workspaces": [
    "bot",
    "webapp"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:bot\" \"npm run dev:webapp\"",
    "dev:bot": "cd bot && npm run dev",
    "dev:webapp": "cd webapp && npm run dev",
    "build": "npm run build:bot && npm run build:webapp",
    "build:bot": "cd bot && npm run build",
    "build:webapp": "cd webapp && npm run build",
    "db:generate": "cd bot && npm run db:generate",
    "db:migrate": "cd bot && npm run db:migrate",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

